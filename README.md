# FilmApp

A simple webapp that lets the user search for a film (through the OMDB API) based on it's title and displays only a single result.

## Run the webpage

Requires XAMPP and run via localhost
